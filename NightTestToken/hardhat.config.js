import '@nomicfoundation/hardhat-toolbox';

const config = {
  solidity: {
    version: '0.8.9',
    settings: {
      optimizer: {
        enabled: true,
      },
    },
  },
};

export default config;
